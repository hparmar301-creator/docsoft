version: '3.8'
services:
  app:
    build: .
    ports:
      - '5000:5000'
    environment:
      - NODE_ENV=production
      - PORT=5000
    restart: unless-stopped

  # Optional: Add database service
  # mongodb:
  #   image: mongo:5
  #   ports:
  #     - '27017:27017'
  #   volumes:
  #     - mongodb_data:/data/db
  #
  # volumes:
  #   mongodb_data:
